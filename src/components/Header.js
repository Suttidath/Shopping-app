import React from "react";
import HeaderCard from "./HeaderCard";

const Header = () => {
  return (
    <header className="header">
      <h1>Genail-Room Shop</h1>
      <HeaderCard />
    </header>
  );
};

export default Header;
